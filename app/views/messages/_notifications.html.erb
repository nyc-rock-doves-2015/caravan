<% @notifications.each do |notification| %>
  <div class="row">
    <div class="col-md-3">Caravan Notification</div>
    <div class="col-md-6"><%= notification.body%></div>
    <div class="col-md-3"><%= notification.created_at %></div>
  </div>
<% end %>