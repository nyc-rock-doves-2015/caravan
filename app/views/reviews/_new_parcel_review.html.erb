<%= form_for [@parcel,@review] do |f| %>
  <%= f.label :rating%>:
  <%= f.text_field :rating %><br>

  <%= f.label :content %>:
  <%= f.text_area :content %><br>

  <%= f.hidden_field :reviewee_id, :value => @parcel.sender_id %>
  <%= f.submit %>
<% end %>