<% if @user.received_reviews.count == 0 %>
  <h4><%= @user.username%> has no reviews</h4>
<% else %>
    <div class="col-md-3"></div>
    <div class="col-md-9">
      <div class="profile-content">
        <h4>Reputation <%= @user.reputation %> / 5</h4>
        <h5>Reviews</h5>
        <%= render partial: '/reviews/show', locals: {reviews: @reviews} %>
    </div>
  </div>
<% end %>